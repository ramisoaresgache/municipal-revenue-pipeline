services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: municipal_revenue
      POSTGRES_USER: portfolio
      POSTGRES_PASSWORD: portfolio
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U portfolio -d municipal_revenue"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:

